import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    default: {
    },
    bold: {
        fontWeight: "bold"
    },
    selectedUser: {
        color: "green"
    },
    backgroundColor: {
        background: "coral"
    },
    
});
